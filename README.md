# 瑞軒 2026 家庭日 — 解謎闖關遊戲（綠世界生態農場）

<p align="center">
  <img src="familyday-frontend/public/images/enroll-welcome-alpaca-adventure-wide-headroom.png" alt="瑞軒 2026 家庭日 — 綠世界闖關活動主視覺（羊駝與熱帶雨林插畫；與 Welcome 頁相同資產）" width="720" />
</p>

## 這是什麼

現場**報到**與**闖關**合一的 Web App（Vue 3 + Vite + Firebase）。細節、截圖、技術圖、目錄表與長篇待辦條列見 **[`docs/overview/root-readme-supplement.md`](docs/overview/root-readme-supplement.md)**。

---

## 快速開始

<a id="quick-start"></a>

需求：**Node.js 20+**。

```bash
cd familyday-frontend
npm install
npm run dev
```

埠號以 [`fdgw.project.json`](fdgw.project.json) 為準（預設常為 `5173`）。

**Windows 一鍵（前端 + API + 雲端 Firestore）**、環境變數與故障排除：[`docs/setup/README.md`](docs/setup/README.md)、[`docs/setup/local-firestore-gcp.md`](docs/setup/local-firestore-gcp.md)。

**Firebase Hosting — 並行兩專案（2026-05-21 起）**

| 環境 | Project ID | Hosting URL | Functions URL |
|------|-----------|-------------|---------------|
| **正式** | `familyday-greenworld` | **[https://familyday-greenworld.web.app](https://familyday-greenworld.web.app)** | `api-jwvq2npioq-uc.a.run.app` |
| 測試 | `rare-lattice-495009-i9` | [https://rare-lattice-495009-i9.web.app](https://rare-lattice-495009-i9.web.app) | `api-hxe6k6ncza-uc.a.run.app` |

**正式環境入口：**

| 用途 | URL |
|------|-----|
| **報到** | [https://familyday-greenworld.web.app/check-in](https://familyday-greenworld.web.app/check-in) |
| **闖關**（首頁／活動入口） | [https://familyday-greenworld.web.app/](https://familyday-greenworld.web.app/) |
| **領獎入口** | [https://familyday-greenworld.web.app/reward](https://familyday-greenworld.web.app/reward) |

部署指令對應：`firebase deploy ... --project=production`（正式）／ 不帶 `--project`（測試 default）。**現場入口 QR：** 已內建於 [`familyday-frontend/public/qr-entry-links/`](familyday-frontend/public/qr-entry-links/)（`entry-check-in.png` / `entry-game.png` / `entry-reward.png`，已重產對應正式 host）；部署後亦可自 `/qr-entry-links/…` 下載列印。詳見該目錄 `README.md` 與 [`docs/setup/hosting-public-entry-urls.md`](docs/setup/hosting-public-entry-urls.md)。

核心入口：

| 用途 | 連結 |
|------|------|
| **全系統架構圖（前端 + API + 後端）** | [`docs/architecture/system-architecture.md`](docs/architecture/system-architecture.md) |
| 需求／待辦／狀態（主文件） | [`docs/project/project-master.md`](docs/project/project-master.md) |
| 本機與預覽 | [`docs/setup/README.md`](docs/setup/README.md) |
| API v0.1 | [`docs/api/api-v0.1.md`](docs/api/api-v0.1.md) · [`docs/api/CHANGELOG.md`](docs/api/CHANGELOG.md) · 索引 [`docs/api/README.md`](docs/api/README.md) |
| `docs/` 索引 | [`docs/README.md`](docs/README.md) |

---

<a id="readme-live-progress"></a>

## 即時進度（工作來源之一）

> **工作約定：** **本表**為**進度現況**之**權威**來源（View／API 函式數／測試檔數等）。**待辦勾選與完整行動清單**以 [`docs/project/project-master.md` § 待辦事項](docs/project/project-master.md#work-backlog) 為準。

> **計數／端點：** 2026-05-11 依原始碼掃描（仍有效；後端 **19** 端點、前端 **10** View／**11** API 函式／**16** 測試檔）。**2026-05-13** 起 **文件完整對齊**：**SPA** **`Authorization: Bearer`** + **`sessionStorage`**；契約 **`api-v0.1` v0.1.25–v0.1.26**（含 **`POST …/auth/login`** 回 **`ok`**／**`token`**／**`user`**，見 **`familyday-backend/src/routes/auth.ts`**）；**`system-architecture` v1.5**（§6.3 新增外部 QR scanner 序列）；**`api-integration-checklist`** Bearer 前置與 §7 說明；**`project-master` v1.3.56**。**同日：** **Firebase Hosting 上架後**已做**全端手動驗收**——**報到（check-in）**與**闖關**主流程之功能與操作**符合需求**（紀錄見 [`docs/testing/api-integration-checklist.md`](docs/testing/api-integration-checklist.md) §7、`docs/testing/api-integration-history.md`）。背景見 [`docs/setup/ios-mobile-auth-fix-2026-05-13.md`](docs/setup/ios-mobile-auth-fix-2026-05-13.md)。**admin/reports**：**`total`／`checkedIn`**、**`progress`** 占位見 [`familyday-backend/src/routes/admin.ts`](familyday-backend/src/routes/admin.ts)。**2026-05-19：** Firestore IAM 憑證設定完成（SA JSON）；**`npm run verify:firestore`** 對正式 Cloud Functions 端點執行，四集合讀寫 **PASS**；前後端重新部署（Functions `api-hxe6k6ncza-uc.a.run.app`、Hosting `rare-lattice-495009-i9.web.app`）；**報到（check-in）＋闖關（game）功能於正式環境驗證完成**；**`project-master` v1.3.55**。**2026-05-20：** 外部 QR scanner 相容（站台 PNG 改編為 `https://<host>/scan?t=<JWT>` 深連結 + SPA `/scan` dispatcher）；闖關紀錄保留（`RegisterView` 登入不再 `restartPlaythrough`、`applyAttemptResult` 移除 stage-1 auto-reset，全破玩家再登入與重玩皆保留 6/6 ✓）；前後端重新部署；commit `4bdfb5d`；**`project-master` v1.3.56`**。**同日（合併部署）：** ResultView 三向分流（首次全破 → 領獎、重玩答對 → 關卡列表）；StageView 6/6 常駐「前往領取闖關禮」CTA；決策抽離至 `src/lib/resultAction.ts`（6 unit tests）；commit `08d60ee`；**`project-master` v1.3.57`**。**同日：** 新增 `/reward` 入口路由 + `entry-reward.png`（領獎台 QR），對應 `pendingFinish` 旗標讓未登入掃描者登入後直接落地 `/finish`；qr-entry-links 共 3 張入口 QR（報到 / 闖關 / 領獎）；**`project-master` v1.3.58`**。**同日：** FinishView 領獎防誤領 — 點「領取闖關禮」改全屏掃 QR，掃到 `fdgw-claim-token` 才呼叫 `/me/reward/claim`；工作人員手持 QR `public/qr-staff-stations/claim-token.png`；解析 `src/lib/claimPayload.ts`（6 unit tests）；**`project-master` v1.3.59`**。**同日：** Hero 視圖 fullBleed —`App.vue` 對 `route.meta.fullBleed` 跳過 90vw 白卡與 safe-area padding，WelcomeView/CheckInWelcomeView 背景圖鋪滿 viewport；WelcomeView 中間換過 `01_welcomeBG.jpg` 9:16 高解析後**復原**至原 `game-welcome-enroll-04.jpg`（commit `6ce9b52`）；BriefingView 地圖換 `02_mapIllu.png`；**`project-master` v1.3.60`**。**同日：** 設計師原版 icon 套用 — StageView 闖關進度 icon 換為山+旗 silhouette、FinishView 領獎狀態 icon 換為獎章+星+雙緞帶 silhouette、領獎三格 slot 改用 `Icon_gift_s_active.png` / `Icon_gift_s_disabled.png`（commit `e597632`）；**`project-master` v1.3.61`**。**2026-05-21：** 正式 Firebase 專案 `familyday-greenworld` 並行上架（Hosting `familyday-greenworld.web.app`、Functions `api-jwvq2npioq-uc.a.run.app`、Firestore 已匯入 111 筆 zh roster）；`.firebaserc` 新增 `staging`/`production` aliases；backend `src/utils/store.ts::getDb()` 修正預設 Firestore database 取得（避免 5 NOT_FOUND）；entry-link QR PNG 已重產對應正式 host；登入功能於正式環境驗證 Pass；commit `acf112b`；**`project-master` v1.3.62`**。**同日：** `PlayerProgress` 加 `rewardRedeemAt: string[]`，每次領獎成功時 push 當下 ISO 時間並寫回 Firestore，提供活動日資料紀錄表（領獎時間一/二/三）直接資料源；無 REST 契約變更；commit `b8e0822`；**`project-master` v1.3.63`**。**2026-05-22：** FinishView 全領完狀態（`isClaimFull` && 3/3）移除「返回首頁」按鈕——同步清掉 `goHome()` handler、`useRouter`／`logoutGame` imports、`actionLoading`／`actionError` refs 與 `finish.backHomeButton` i18n key；commit `c4740e5`；正式環境 `familyday-greenworld.web.app` 部署完成並驗證；**`project-master` v1.3.65`**。**2026-05-25：** 全畫面 fullBleed——`App.vue` 移除外層 90vw 圓角白卡與 safe-area padding 包裝，所有 view 之 root `bg-[#xxx]` 直接鋪到 viewport 邊；`route.meta.fullBleed` 機制與兩處冗餘旗標、`PageCritters` viewport 引用一併移除；bundle 縮 `index.js 310→306 kB`／`index.css 47→45.5 kB`；commit `64abdc2`；正式環境部署完成並驗證；**`project-master` v1.3.66`**。**同日：** 外部 QR 未登入彈窗——`/scan` redirect 不再分流，有無 session 都導 `/quiz?challengeId=<N>`；`QuizView` mount 偵測 `!getSessionToken() && getPendingStationVerification()` → 在前景 mount `StageScanLoginModal`（白卡 + 勾選 icon + 標題「登入帳號開始答題」+ 員編/姓名 input + 確定登入 CTA），無取消鍵、無 backdrop dismiss、唯一退路是登入成功；登入過程不動 `completedStageIds`；i18n 新增 `scanLogin.title`／`scanLogin.submitButton`；router test 同步；commit `fa982ba`；正式環境部署完成並驗證；**`project-master` v1.3.67`**。**同日：** 正式環境 roster 全面替換——舊 111 筆 staging seed test data 已 purge、新 508 筆真實活動資料 import 完成；活動日相關集合（checkins / player_progress / redeem_*）保持 0；過程修了 5 個 admin script 對 production unnamed default Firestore db 的 `5 NOT_FOUND` bug（commit `05c667f`，對齊 `store.ts::getDb()` 解析邏輯）；新增 `docs/setup/local-firestore-gcp.md` §E「對正式環境執行 admin CLI」runbook，明確記載 `FDGW_FIREBASE_PROJECT_ID=familyday-greenworld` 覆寫的必要性；**`project-master` v1.3.68`**。**2026-05-26（dev 全部變更首次完整部署至正式並收尾）：** ① 站 1 名稱 dashboard config 由「水鳥區」對齊「天鵝湖」（與 stage-1 題目／前端一致），重部署 `functions:api`；commit `7a0fc66`。② 相機掃碼登入連續性修正——session token 改存 **`localStorage`**（同瀏覽器跨分頁共享，免重登）＋新增 **`GET /api/v1/auth/me`** client（`src/api/authMe.ts`）＋`QuizView`／`FinishView` 以 `/auth/me` 回填 profile，解決「相機開新分頁被迫重輸員編登入」與「finish 頁顯示預設名『夥伴』」；hosting 重部署；commit `17eeedb`。③ 正式環境 app 集合歸零——purge `checkins`／`player_progress`（保留 508 roster、redeem 維持 0），清除前期測試污染。④ stage 1 題目改為《天鵝湖》作曲家題、正解 **C（柴可夫斯基）**：後端 `state/game.ts` `c1 correctChoiceId` `B→C`＋前端 `stageQuestions.ts` 題庫＋對應單元測試；`functions:api`＋hosting 重部署；commit `384c78c`。測試計數更新：前端 **19** 檔／**131**、後端 **5** 檔／**29**；**`project-master` v1.3.69`**。**2026-05-29（壓測管線上線並完成 1,300／3,000 VU 雙輪驗證）：** 後端 `src/index.ts` 側掛 **`apiLoadtest`** export（同專案下用 `loadtest` 具名 Firestore database，per-request 覆寫 `FDGW_FIRESTORE_DATABASE_ID`，正式 `api` 與 `(default)` db 完全不干擾）；新增 3 支壓測腳本 `scripts/{generate-loadtest-roster,loadtest-consistency-check,delete-player-progress}.mjs`。**k6 第一輪 1,300 VU × 10× hot-doc**（員編池 130、ramp 5/sustain 10/ramp-down 3 min）：checks **99.99%**、`http_req_failed` **0.00%**、p(95) **1.0 s**、428,232 reqs、Cloud Run 自然 scale up；post-test 一致性檢查 130 個 `player_progress` doc 全 `[1..6]`、**0 partial → 0 RMW race**。**同日加碼 3,000 VU × 23× hot-doc 極限測試**：checks **99.96%**、failed **0.03%**、p(95) **1.06 s**、969,502 reqs、873 RPS（線性 scale），同樣 **0 partial doc**——Firestore 超出 1 wps/doc soft limit 至 2.4 wps 仍未見 race。額外驗證 `/me/playthrough/restart` 不重置 `rewardRedeemCount`，但用 `delete-player-progress.mjs` 刪 doc → 下次 `getOrInitProgress` 以 `defaultProgress()` 重生 → **不改 backend code 即可達成「玩家硬重置」**；commit `f8ccad3`；**`project-master` v1.3.70`**。**同日(壓測完當天稍晚收尾):** 補上正式環境**監控與預算告警基線**——5 條 GCP 告警全部設定並驗證,全部寄到 `familyday.amtran@gmail.com`(透過 `familyday-amtran-primary` notification channel):① **Budget alert $50/月**（50%/90%/100% Actual 三段）;② **Hosting Uptime check**（`familyday-greenworld.web.app`,15 min × 6 region）;③ **API Uptime check**（`api/v1/health`,同配置）;④ **Cloud Run failure rate**（`api` 5xx 5 min 內 > 10）;⑤ **Cloud Run p95 latency**（`api` p95 > 2 s 持續 5 min）。Channel verification email 已驗證,end-to-end 通路 PASS。runbook 入版 [`docs/setup/monitoring-and-alerts.md`](docs/setup/monitoring-and-alerts.md)（commit `3ba0dba`）;`project-master` 第 418 行「盤點正式上線前必要監控與預算告警門檻」勾掉;**`project-master` v1.3.71**。**同日(收尾完再加碼):** **Cloud Functions runtime 升級 `nodejs20 → nodejs22`**——`familyday-backend/firebase.json` `runtime` 與 `package.json` `engines.node` 同步;後端 vitest 5 檔/29 通過、`tsc build` OK;`firebase deploy --only functions:api,functions:apiLoadtest --project=production` 兩個函式都 **`Updating Node.js 22 (2nd Gen)`** Successful;部署輸出**不再有 nodejs20 棄用警告**;smoke 驗證正式 `api`(`8411007` 吳春發 query + login chain)與 `apiLoadtest`(`LT900001` query)皆 200,Firestore 讀取鏈未斷;**避開 2026-10-30 decommission deadline**(提前 5 個月處理);commit `3e2c95d`;**`project-master` v1.3.72**。**2026-06-02(報到表單需求修正):** ① `CheckInFormView` 員工編號欄 placeholder 由 `例如：1141041` 改成 `例如：1141157`(`src/i18n/zh-TW.ts`);② 同行人數下拉範圍 `[1..MAX]` 擴成 `[0..MAX]`,**預設選「0 人 (沒有攜伴)」**;`demoState.getCompanionCount/setCompanionCount` 下限同步 1→0、新 i18n key `companionUnitZero`、formValid 與 commitCheckIn 守 `>= 0`、確認頁 0 → 「0 人 (沒有攜伴)」。後端 `utils/http.ts` 新增 `toNonNegativeInt(v, fallback=0)` helper、`routes/checkin.ts` 改用之並把缺值判斷從 `!partySize`(舊邏輯誤擋 0)改為「undefined/null/空字串/負數/NaN 才擋」。`functions:api + functions:apiLoadtest + hosting` 三件部署(過程踩到 `tsc` 增量編譯 cache 導致 `lib/utils/http.js` 沒更新的坑,`rm -rf lib && npm run build` 強制重建後修正);apiLoadtest smoke:`partySize=0` → 200 寫入 0、缺值 → **400 INVALID_CHECKIN_PAYLOAD**、`partySize=3` → 200 寫入 3。前後端測試 5/29 + 19/131 全綠;commit `1efdbf6`。**`project-master` v1.3.73**。**同日(briefing 頁文案＋6 站名重新命名,對齊設計稿 v9):** 依 `ref_pic/image copy 29.png` 三條修法同 commit 處理:① `/briefing` 頂部歡迎段落整段刪除(i18n key `briefing.intro` + `BriefingView.vue` 對應 `<p>` 一併移除);② 第一條 gift bullet 從「輸入姓名及員工編號,開始你的冒險!」改為「輸入員工編號並確認姓名,開始你的冒險。」;③ 站名重命名(stage 1 不動,2~6 全改) — 2 大探奇區→**可愛動物區**、3 水生植物公園→**雨林空中步道**、4 鳥園→**羊駝之家**、5 蝴蝶園→**蝴蝶生態公園**、6 生物多樣性探索區→**鳥類生態公園**。三處同步:`i18n/zh-TW.ts::briefing.locationsList`、`lib/demoState.ts::STAGE_NAMES`、`familyday-backend/fdgw.project.json::dashboard.stages[].title`,確保 `/briefing` 顯示、`/stage` `/quiz` 站名 badge、與 `GET /me/dashboard` payload 三邊都一致。`demoState.test.ts::stageTitle` assertion 從只測 stage 1 + 99 fallback,擴成測 1~6 全 6 站名(防未來單一檔被改卻沒同步另兩邊)。無 API 契約變更、無 Firestore schema 變更;`stageQuestions.ts` c2~c6 是通用生態題不綁站名,不受影響。`functions:api + functions:apiLoadtest + hosting` 三件部署、`hosting` 兩個新檔上傳(新 `index.html` + 新 `index-BTiK1fJS.js`);live 驗證:hosting bundle hash 對上 build、apiLoadtest dashboard 回傳 6 個新站名完全正確;commit `a470bf9`。**`project-master` v1.3.74`**。**同日(hotfix:正式 SPA login 與 challenges 全壞,還原)::** 上一輪 `a470bf9` 之後使用者實測登入失敗 + `/quiz` 載入題目失敗,根因雙重 — ① 我用 `cd familyday-frontend; npm run build` 直跑,Vite 載入 `.env.local`(本機 emulator URL `http://localhost:5003/...`)烤進 production bundle → 所有 fetch 跑去打 localhost,瀏覽器當然失敗,bundle audit 0 個 Cloud Run URL ref、1 個 `localhost:5003` ref;② `fdgw.project.json::productionApi.viteApiBase` 自 2026-05-21 開正式 project 以來一直錯指向 **staging** 函式 URL `api-hxe6k6ncza-uc.a.run.app`,即使走對 build 流程也烤錯 host。修法:同步把 `productionApi.viteApiBase` 校正成 `https://api-jwvq2npioq-uc.a.run.app`(正式 Cloud Run host),改走 `node ./scripts/build-frontend-for-hosting.mjs`(它會用 process.env.VITE_API_BASE 覆蓋 `.env.local`)+ `sync-hosting-dist.mjs` + `firebase deploy --only hosting`。新 bundle `index-DzrEG1bC.js` audit:**production URL 1 個 ref、staging URL 0、localhost 0**;live 驗證 `POST /auth/login {employeeId:8411007, name:"吳春發"}` → 200 + `ok:true`、`GET /challenges/c1` 200。無後端代碼變更,函式 revision 未動。同類事故 2026-05-12 已修過一次(commit `ab4edf5` 就是為了避這個雷而新增 `build-frontend-for-hosting.mjs`),這次重蹈覆轍,memory 補上 `feedback_vite_api_base_trap` 防再犯;commit `4985321`。**`project-master` v1.3.75`**。**同日(設計稿 v9 後續批次 image copy 30–35:報到/闖關/題庫/領獎頁多項文案與 UI 調整,6 筆逐項 hosting-only 部署並 live 驗證):** ① **闖關登入頁**(RegisterView 與 StageScanLoginModal 共用 key)員編 placeholder `例：1110000`→`例如：1141157`,對齊報到表單;commit `6a04aed`;v1.3.76。② **闖關進度頁**(StageView 到站前區塊,image copy 31)改版:排列改為 hero圖→大標→說明文字→按鈕、圓形相機 icon 按鈕換成設計師完整圖檔按鈕(`public/images/scan-qr-button.png`+`@2x` retina、文字+相機 icon 內嵌,常數 `SCAN_QR_BUTTON_SRC`/`_2X`,`<img srcset>` 包在既有 `<button>` 內、`openScanUi()` 與掃碼全屏邏輯不動)、`stage.heroSubtitle` 改「【不限闖關順序】前往任意關卡,點擊下方相機掃描 QR Code 開始挑戰!」、aria-label 改「點我掃描QRcode」;6 站名沿用 `a470bf9`;commit `5b22359`;v1.3.77。③ **題庫**(image copy 32):第 2 關(可愛動物區)換水豚君題(正解「泡在水裡或洗溫泉」)、第 4 關(羊駝之家)換羊駝吐口水題(正解「對你吐口水」),正解皆置於 (B)/index 1 → 對上後端既有 `c2=B`/`c4=B`,**後端零變更**,僅動 `stageQuestions.ts`;commit `c3bfb25`;v1.3.78。④ **領獎頁文案精簡**(image copy 33):`finish.completeMessage` 去掉「恭喜您完成所有關卡,」前綴→「請至服務台領取闖關禮。」;刪除領取按鈕上方 staff hint(`finish.staffHintClaim`「請交由工作人員點選領取」)之 `<p>` 與已不用的 i18n key,後續 `rewardWaitingBanked` 由 `v-else-if` 改 `v-if`;commit `7ec5a9c`;v1.3.79。⑤ **達上限文案**(image copy 34):`finish.rewardLimitReached`→「您已達闖關禮領取上限(共 {maxSlots} 次),仍可繼續闖關體驗。」({maxSlots} 佔位保留;`claimSuccess.rewardLimitReached` 另頁不同訊息不動);commit `0315088`;v1.3.80。⑥ **未通關進領獎頁**(image copy 35):接出後端 `GET /me/dashboard` 既有 `progress.allCompleted` 欄位(=`completedStageIds.length >= total`)——`rewardClaimStatus` payload + `rewardClaimPresentation` result 透傳(API 帶真值、mock-query/local-fallback 設 `true` 保留預覽慶祝頁),FinishView `notYetCleared = statusLoadState==='ok' && !allCompleted` → 標題改「闖關尚未完成」(`finish.headlineIncomplete`)+ 訊息「完成全部關卡挑戰,即可領取闖關禮。」(`finish.incompleteMessage`)+ 隱藏 `rewardWaitingBanked` 警告框;**「恭喜您完成闖關」僅在 6/6 全破時顯示**(已通關待領/已領完/待下一輪等狀態畫面不變),**後端零變更**;commit `75ec38a`;v1.3.81。本批每筆 bundle audit 皆 **prod=1/staging=0/localhost=0**、live bundle hash 對齊 build、functions 未重佈;前端測試自 **131 增至 132**(+1 `rewardClaimStatus` 的 `allCompleted` 解析測試,`rewardClaimPresentation` 的 `toEqual` 物件同步加欄位)。**`project-master` v1.3.81`**。**2026-06-03(bugfix:全破後重新掃 QR 重玩,結果頁誤顯示「闖關完成頁面」領獎鈕 — 改後端權威判定 `justFullCleared`):** 根因 — `QuizView.confirm()` 以本地 **sessionStorage** `completedStageIds` 重算「首次全破」(`priorCompletedLen < 6 && new >= 6`),但**關閉所有分頁會清空 sessionStorage**,經 QR + cookie session / scan-login modal 重新進入時 `onMounted` 只回填姓名、**不同步後端進度**(唯一同步點 `syncLocalProgressFromDashboard` 僅 `RegisterView` 登入表單呼叫),導致 `priorCompletedLen=0` → 全破玩家重玩被誤判為首次全破 → ResultView 走「領獎」分支。修法(**方案 A:單一權威來源**) — 後端 `applyAttemptResult`/`attemptSnapshot` 新增回傳 **`justFullCleared`**(與 `bankedFullClears += 1` 同一條件:`prior < 6 && 本次後 >= 6`,全破重玩恆 `false`),`routes/game.ts` response 輸出之;前端 `gameFlow.AttemptJson` + `submitChallengeAttempt` 接出,`QuizView` 改 `firstClear = r.correct && r.justFullCleared`、移除本地 `priorCompletedLen` 重算與未用的 `getCompletedStageIds` import。**契約變更(加性、向後相容)**:`POST …/attempts` 回應多 `justFullCleared`(見 `api-v0.1` **v0.1.27** §7 + `CHANGELOG [Unreleased]`)。測試:後端 +1(`game.test.ts`:前 5 關 `false`、第 6 關 `true`、全破重玩 `false`)→ 5 檔/**30**;前端 +1(`gameFlow.test.ts` 接出欄位)→ 19 檔/**133**;`game.test.ts` 答錯案例 `toEqual` 同步加欄位。部署 **`functions:api + functions:apiLoadtest + hosting`**(後端有變動):兩函式 `Node.js 22` 更新成功、hosting bundle `index-Ci2-hUYN.js`;**live smoke**(`apiLoadtest`/`loadtest` db,`LT900001` 已全破)→ 重玩 c1 回 `allStagesCompleted:true` 但 **`justFullCleared:false`**,精準重現並驗證修復;commit `497fa0b`。**`project-master` v1.3.82`**。**2026-06-04(新增每日自動匯出:Firestore checkins/player_progress → CSV 寄信,全雲端):** 需求 — 每天自動把 `(default)` 的當天資料 dump 寄到 `familyday.amtran@amtran.com.tw`。做法用 **Firebase Functions v2 `onSchedule`**(`dumpCheckinsDaily`,`us-central1`,**每天 08:00 Asia/Taipei**,部署時自動建 Cloud Scheduler job + Pub/Sub,**不需本機觸發**);讀 `(default)` 的 `checkins` + `player_progress`(姓名由 `roster` 依員編 join)→ 產**兩個 UTF-8 BOM CSV**(對齊「家庭日當天資料紀錄表.xlsx」兩 sheet:**報到紀錄表** 工號/姓名/報到時間/攜伴人數、**闖關遊戲紀錄表** 工號/姓名/領獎次數/領獎時間一二三,時間轉台北)→ `nodemailer` 經 Gmail SMTP(App 密碼存 **Secret Manager `GMAIL_APP_PASSWORD`**,不進 git)寄出。程式碼分層:`scheduled/dumpCheckins.ts`(onSchedule I/O)、`dumpReport.ts`(`buildDailyDumpReport` 純函式組裝主旨/內文/兩 CSV)、`checkinCsv.ts`/`progressCsv.ts`(各 sheet 純 builder);後端測試 5→**8** 檔、30→**48**(新增 `checkinCsv`/`progressCsv`/`dumpReport`,鎖住欄位/join/排序/時區/容錯)。部署 `functions:dumpCheckinsDaily` 成功(自動啟用 cloudscheduler API、授予 runtime SA 讀 secret);以 SA(`.secrets/`)REST 強制執行 job 兩次驗證,log 皆 `dumpCheckinsDaily sent`(報到 2／闖關 3),實機收信兩附件格式正確。runbook 入版 [`docs/setup/scheduled-checkins-dump.md`](docs/setup/scheduled-checkins-dump.md)。新增相依 `nodemailer`;commit `fef59a4`。**`project-master` v1.3.83`**。**2026-06-05(新增一次性自動下架:活動結束自動對外關閉前後端,全雲端):** 需求 — **2026-07-17 23:00 (Asia/Taipei)** 自動把前後端對外關閉(省成本非重點,idle 本就 scale-to-zero;重點是活動後對外關站)。做法 **Firebase Functions v2 `onSchedule`**(`scheduledTeardown`,cron `0 23 17 7 *`、Asia/Taipei,部署自動建 Cloud Scheduler job),以**專用最小權限 SA `fdgw-teardown@…`**(只授 `firebasehosting.admin`/`run.admin`/`cloudscheduler.admin` 三角色,對應三動作)身分執行;觸發時:① Hosting `SITE_DISABLE` release(前端停用頁)② 移除 `api`/`apiloadtest` 的 `allUsers` `run.invoker`(API 全 403)③ 暫停 `dumpCheckinsDaily` 排程(停每日 dump 信);**資料保留不動 Firestore**。程式碼 `scheduled/scheduledTeardown.ts`(onSchedule 指定 serviceAccount)+ `teardownActions.ts`(`runTeardown(dryRun)` + 純函式 `withoutPublicInvoker`);復原 `scripts/restore-production.ps1`(重佈 functions 還原 invoker/dump 排程 + hosting 覆蓋 SITE_DISABLE)。**IAM 限制**:`.secrets/` 的 firebase-adminsdk SA 能建 SA 但無權改專案 IAM → 三角色 + actAs 由專案 Owner(`familyday.amtran@gmail.com`,本身 Owner 已隱含 actAs)在 Console 授權。部署 `functions:scheduledTeardown` 成功(以 fdgw-teardown 身分、ENABLED 排程 `0 23 17 7 *` Asia/Taipei)。**安全實測**:模擬 fdgw-teardown 身分對 `apiloadtest` 移除→還原 invoker(`[allUsers]`→`[]`→`[allUsers]`)、對 dump 排程 pause→resume(PAUSED→ENABLED)皆 200 並復原,正式 `api` 與 hosting 未動。後端測試 8→**9** 檔、48→**54**(新增 `teardownActions`:invoker 轉換 + dry-run 計畫)。runbook 入版 [`docs/setup/scheduled-teardown.md`](docs/setup/scheduled-teardown.md);新增相依 `google-auth-library`;commit `7a1900b`。**`project-master` v1.3.84`**。

| 面向 | 現況 |
|------|------|
| 前端 | 10 個 View **全數完整實作**（WelcomeView → FinishView；舊 **`/finish/claimed`** 僅 redirect **`/finish`**）；API 層 12 支函式對應真實端點（含 `auth/me`）；19 個測試檔無 skip/stub；CI 通過；**登入態以 `localStorage` token 跨分頁共享**，呼叫同送 `Authorization: Bearer`＋cookie（`credentials:include`），未登入時 `/auth/me` 探測回填身分（`src/lib/sessionToken.ts`、`src/api/authMe.ts`）；**外部 QR scanner 進入點 `/scan`**（`src/lib/scanEntry.ts` + `qrPayload.ts`） |
| API | **19** 個端點均已實作。**`GET /admin/reports/attendance`**：**`total`**＝Firestore **`roster`**（現行 **`eventId`** 下名冊筆數）；**`checkedIn`**＝**`checkins`** 計數。**`GET /admin/reports/progress`**：**`redeemed`** 取自核銷摘要；**`players`／`fullClear`** 仍為 **`admin.ts`** 占位常數（待對齊 `player_progress` 聚合） |
| 後端資料層 | roster / checkins / player_progress / redeem 四集合已完整實作 Firestore + in-memory 雙模式切換；**Firestore IAM 憑證已設定**（2026-05-19） |
| 測試 | 前端 Vitest **19** 檔／**133** 測試通過（含 `qrPayload`／`scanEntry`／`/scan`／`/reward` redirect／`resultAction`／`claimPayload`／`authMe`／`rewardClaimStatus.allCompleted`／`gameFlow.justFullCleared`）；後端 Vitest **9** 檔／**54** 測試通過（含 `justFullCleared` 語意、每日匯出 `checkinCsv`／`progressCsv`／`dumpReport`、一次性下架 `teardownActions`）；`verify:firestore` 正式端點 **PASS**（2026-05-19）；**報到＋闖關＋ResultView 三向分流＋領獎入口＋FinishView 防誤領掃 QR 全部於正式環境手動驗證 Pass**（2026-05-19、2026-05-20） |
| 部署 | **正式 `familyday-greenworld.web.app` 為現行主環境**（Functions `api-jwvq2npioq-uc.a.run.app`）；長期壓測入口 **`apiLoadtest`**（同專案、`loadtest` 具名 db、idle 0 instance 不計費）；2026-05-26 已將 `dev` 全部變更部署並驗證、app 集合歸零（roster 508 保留）；測試環境 `rare-lattice-495009-i9.web.app`（`api-hxe6k6ncza`）保留；正式上線仍須完成 Security Rules（`firestore.rules` 仍 deny-all）／安全基線（**1,300 人 k6 壓測已於 2026-05-29 PASS** ✓） |

---

## 待辦與進度（摘要）

- **待確認／里程碑表／技術選型細節／下一步完整條列** → [`docs/overview/root-readme-supplement.md#backlog-detail`](docs/overview/root-readme-supplement.md#backlog-detail)
- **主文件（權威待辦）** → [`docs/project/project-master.md` § 待辦事項](docs/project/project-master.md#work-backlog)

**本週摘要（與主文件對齊；勾選以 `project-master` 為準）**

- **高優先：** ~~Firestore IAM、`verify:firestore`~~（**已完成 2026-05-19**）、~~六張正式 QR（JWT）`public/qr-staging-stations/`~~（**已完成 2026-05-19**）、安全基線確認單（**Bearer／XSS**、CORS、Security Rules）
- **中優先：** runbook、Security Rules、後端 checkin／redeem／admin 自動化擴充、~~補強 `GET …/admin/reports/progress`（`players`／`fullClear`）聚合~~（**已完成**：`state/game.ts` `getProgressSummary()` 已對接）
- **低優先：** 見主文件

---

## 對外連結錨點（相容舊連結）

外部文件若仍使用**舊**根目錄 `README.md#…` 錨點，請改連下列**新位置**（內容已集中在補充篇）。

| 錨點 | 說明與新位置 |
|------|----------------|
| [`#preview-netlify-test-ui`](docs/overview/root-readme-supplement.md#preview-netlify-test-ui) | 測試 Web UI／Netlify |
| [`#ui-preview-screenshots`](docs/overview/root-readme-supplement.md#ui-preview-screenshots) | 介面截圖 |
| [`#gcp-service-account-local-firestore`](docs/overview/root-readme-supplement.md#gcp-service-account-local-firestore) | GCP 服務帳戶與本機 Firestore |

---

*README v3.04 · 2026-06-05(新增一次性自動下架 `scheduledTeardown`:2026-07-17 23:00 Asia/Taipei 自動對外關閉前後端 — Hosting disable + 移除 Run invoker + 暫停 dump 排程,以專用最小權限 SA `fdgw-teardown` 執行,資料保留;復原 `scripts/restore-production.ps1`;runbook `docs/setup/scheduled-teardown.md`;後端測試 54;`project-master` v1.3.84;前版 v3.03)*
