# 操作示範錄影（Demo）

本資料夾存放**畫面操作錄影**（多為瀏覽器螢幕錄製），與線框靜態圖（[`../design/wireframe/`](../design/wireframe/)）分開管理。**不**放入 `source/public/` 打包進前端。

## 根目錄 `README.md` 內嵌播放

根 `README.md` 以 HTML5 **`<video src="docs/demo/family-day-prototype-demo.mp4">`** 內嵌。**請將主示範檔**複製或更名為：

`docs/demo/family-day-prototype-demo.mp4`

其他檔名可保留備份；README 只引用上述固定路徑。

**預覽環境：** GitHub 倉庫首頁、Cursor／VS Code Markdown 預覽多數可播放；少數檢視器僅顯示後援連結（見根 README「Demo 影片預覽」）。

## 維護

- 檔名建議含日期或版本；大檔可評估 **Git LFS** 或改雲端連結並於此註記 URL。
