# Dependencies & build (future)
node_modules/
dist/
build/
out/
functions/lib/
.venv/
__pycache__/
*.pyc

# Env & secrets
.env
.env.*
!.env.example
familyday-frontend/staging-qr-payloads.local.txt
familyday-frontend/public/qr-staging-stations/
# Never commit Firebase/GCP service account JSON (use repo-external path or team secret store)
secrets/
**/*-sa.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/*
!.vscode/extensions.json
# Claude Code session-local settings (per-machine, do not version-control)
.claude/

# Logs
*.log
npm-debug.log*

# Local design / exports — do not commit
UI_0505/

# Real activity data & reference material — contain PII (roster, screenshots, design assets).
# Public repo, so blanket-ignore by directory name and by spreadsheet extension.
ref_no_push/
ref_pic/
*.xlsx
*.xlsm
*.xls

